<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"

       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">



    <!--1.读取连接数据库、小程序、连接LDAP、教务在线访问地址相关参数的perperties文件-->
    <context:property-placeholder location="classpath:properties/db.properties,classpath:properties/wxMassige.properties,classpath:properties/ldap.properties,classpath:properties/jwzxRequestURL.properties"/>

    <!--2.数据库连接池 数据源-->
    <bean id="druidDataSource" class="com.alibaba.druid.pool.DruidDataSource">
        <property name="driverClassName" value="${jdbc.driver}"/>
        <property name="url" value="${jdbc.url}" />
        <property name="password" value="${jdbc.password}"/>
        <property name="username" value="${jdbc.username}"/>
    </bean>

    <!--3.配置sqlsessionfactory工厂-->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <!--3.1 加载mybatis核心配置文件-->
        <property name="configLocation" value="classpath:mybatis/mybatis-config.xml"/>
        <!--3.2 配置数据库连接池-->
        <property name="dataSource" ref="druidDataSource"/>
    </bean>

    <!--4.扫描mapper接口文件 由mybatis生成mapper接口的实例化对象并交给spring管理     （不用注入sqlsessinfactory会自动寻找工厂）-->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="basePackage" value="cqupt.jyxxh.uclass.dao"/>
    </bean>
</beans>